services:
  bambu-run:
    build: .
    ports:
      - "8000:8000"
      - "8808:8808"
    env_file: .env
    volumes:
      - bambu_data:/app/data
    restart: unless-stopped

volumes:
  bambu_data:
